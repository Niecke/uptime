{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "helpers:pinGitHubActionDigests"
  ],
  "baseBranchPatterns": [
    "dev"
  ],
  "postUpdateOptions": [
    "gomodTidy"
  ],
  "packageRules": [
    {
      "description": "Keep Docker images (Dockerfile + compose) pinned to a digest, and bump the digest when the upstream tag is rebuilt.",
      "matchDatasources": [
        "docker"
      ],
      "pinDigests": true
    },
    {
      "description": "This repo's own image. The release workflow writes the tag and digest into compose.yml the moment it publishes them, so Renovate would only ever raise a PR for a line that is already correct.",
      "matchDatasources": [
        "docker"
      ],
      "matchPackageNames": [
        "niecke11/uptime"
      ],
      "enabled": false
    },
    {
      "description": "Go patch updates are low-risk; batch them into one PR to keep review noise down.",
      "matchManagers": [
        "gomod"
      ],
      "matchUpdateTypes": [
        "patch"
      ],
      "groupName": "go patch updates"
    }
  ],
  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": [
      "security"
    ]
  },
  "osvVulnerabilityAlerts": true
}
